# ============================================================
# OptiScam - Python Dependencies
# ============================================================
#
# IMPORTANT: Install PyTorch with CUDA BEFORE running pip install -r requirements.txt
#
#   Step 1 - Check your CUDA version:
#     nvidia-smi   (look for "CUDA Version" top-right)
#
#   Step 2 - Install PyTorch (choose matching CUDA version):
#     CUDA 12.6:  pip install --force-reinstall torch torchvision --index-url https://download.pytorch.org/whl/cu126
#     CUDA 12.4:  pip install --force-reinstall torch torchvision --index-url https://download.pytorch.org/whl/cu124
#     CUDA 12.1:  pip install --force-reinstall torch torchvision --index-url https://download.pytorch.org/whl/cu121
#     CPU only:   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#
#   Step 3 - Verify GPU is detected:
#     python -c "import torch; print(torch.cuda.is_available()); print(torch.cuda.get_device_name(0))"
#
#   Step 4 - Then install the rest:
#     pip install -r requirements.txt
# ============================================================

# Core ML / Transformers
transformers==4.57.1
accelerate>=0.25.0
bitsandbytes>=0.43.0
sentencepiece>=0.1.99

# Qwen-VL utilities (required for process_vision_info)
qwen-vl-utils>=0.0.8

# Image processing
opencv-python>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0

# OCR
rapidocr-onnxruntime>=1.3.0

# Audio transcription
openai-whisper>=20231117

# Web API (backend server)
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.9

# Note: FFmpeg binary must be installed separately — NOT a Python package.
#   Windows: choco install ffmpeg  OR  scoop install ffmpeg
#   Or download from: https://ffmpeg.org/download.html
#   Verify with: ffmpeg -version
